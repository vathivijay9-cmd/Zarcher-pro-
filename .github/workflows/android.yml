    - name: Build with Gradle
      run: |
        # This forces the build to use 9.3.1, ignoring the properties file cache
        ./gradlew assembleDebug --refresh-dependencies -Dorg.gradle.wrapper.version=9.3.1
